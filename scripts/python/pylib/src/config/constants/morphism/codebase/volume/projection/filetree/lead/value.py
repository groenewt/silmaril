VALUE = "The sections below are the literal file tree of the observed corpus. One heading is one filesystem entry, a child entry is nested inside the scope its parent opens, and the nesting continues to the full depth the path itself declares. Under each file, one element per contributing table carries every fact that table records about that path, with the join that placed it there stated beside it.\n"
