VALUE = (
    b'# Silmaril GeoSPARQL Query Library\n'
    b'# NOTE: The current Silmaril substrate does not contain explicit geospatial\n'
    b'# data, but these queries are ready for when location-aware concrete anchors\n'
    b'# are added (e.g., Tartu Animal Clinic coordinates). They are parse-valid\n'
    b'# SPARQL (gated by scripts/validate-sparql.py); EXECUTING the geof: filter\n'
    b'# functions requires a GeoSPARQL-capable engine (e.g. Apache Jena GeoSPARQL),\n'
    b'# not plain rdflib.\n'
    b'\n'
    b'# GQ1: Find all entities within a bounding box (when geo data is added)\n'
    b'PREFIX geo: <http://www.opengis.net/ont/geosparql#>\n'
    b'PREFIX geof: <http://www.opengis.net/def/function/geosparql/>\n'
    b'SELECT ?entity ?wkt WHERE {\n'
    b'    ?entity geo:hasGeometry ?geom .\n'
    b'    ?geom geo:asWKT ?wkt .\n'
    b'    FILTER (geof:sfWithin(?wkt,\n'
    b'        "POLYGON((24.7 58.3, 24.8 58.3, 24.8 58.4, 24.7 58.4, 24.7 58.3))"^^geo:wktLiteral))\n'
    b'}\n'
    b'\n'
    b'# GQ2: Find entities near a point (buffer search)\n'
    b'PREFIX geo: <http://www.opengis.net/ont/geosparql#>\n'
    b'PREFIX geof: <http://www.opengis.net/def/function/geosparql/>\n'
    b'SELECT ?entity ?wkt WHERE {\n'
    b'    ?entity geo:hasGeometry ?geom .\n'
    b'    ?geom geo:asWKT ?wkt .\n'
    b'    FILTER (geof:sfWithin(?wkt,\n'
    b'        geof:buffer("POINT(24.745 58.378)"^^geo:wktLiteral, 0.01)))\n'
    b'}\n'
)
