from config.constants.morphism.codebase.volume.artifact.manifest.state.bytes.key.value import VALUE as ARTIFACT_BYTES_KEY


def ARTIFACT_BYTES(state: dict) -> dict:
    return state[ARTIFACT_BYTES_KEY]
