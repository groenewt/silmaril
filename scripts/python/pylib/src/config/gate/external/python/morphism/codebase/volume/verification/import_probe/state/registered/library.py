from config.constants.morphism.codebase.volume.verification.import_probe.state.registered.key.value import VALUE as REGISTERED_KEY


def REGISTERED(state: dict) -> list:
    return state[REGISTERED_KEY]
