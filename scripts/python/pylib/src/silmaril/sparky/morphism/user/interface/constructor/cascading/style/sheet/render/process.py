"""Process launcher; active-red migration, not an admitted unary Frame arrow.

The nullary host function returns an integer and inherits process streams.
A successful render establishes projection validation only; it does not prove
physical byte-carrier descent or unary Frame admission.
"""
from config.constants.morphism.user.interface.constructor.cascading.style.sheet.render.process.command.value import VALUE as COMMAND
from config.gate.external.python.stdlib.subprocess.library import DEPENDENCY as SUBPROCESS


def MAIN() -> int:
    return SUBPROCESS.run(COMMAND).returncode


raise SystemExit(MAIN())
