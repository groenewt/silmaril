{
  "status": "active_red_migration",
  "focused_green": [
    "legacy JSON and text projection parity on the pinned four-document fixture",
    "one MAIN and one registered subprocess application per transitional runtime coordinate",
    "external executable evidence hashes"
  ],
  "not_closure_authority": [
    "grounding, schema, delimiter, cardinality, status, and format VALUE leaves are isolated but not yet typed observations admitted from a caller, specification, or artifact",
    "Make recipes still route streams through shell redirection",
    "the public edge does not yet accept exactly one physical request-frame pathname",
    "document-instance parsing JSON summary construction and text rendering still contain more than one semantic transition",
    "typed invocation-frame construction, validation, dispatch, and output/effect/status readback are not yet separate coordinates"
  ]
}
